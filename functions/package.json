{
  "name": "agentflow-functions",
  "private": true,
  "version": "1.0.0",
  "description": "AgentFlow serverless handlers: action handlers, the workflow executor, event and cron consumers.",
  "dependencies": {
    "cron-parser": "^4.9.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.0",
    "typescript": "^5.5.4"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  }
}
