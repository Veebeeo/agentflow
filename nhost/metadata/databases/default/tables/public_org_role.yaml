table:
  name: org_role
  schema: public
select_permissions:
  - role: user
    permission:
      columns: '*'
      filter: {}
    comment: Reference data. Needed by the builder UI to list valid options.
